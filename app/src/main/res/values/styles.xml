<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- No Data Found TextView styling specs. -->
    <style name="no_data_view">
        <item name="android:gravity">center</item>
        <item name="android:text">@string/no_data</item>
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
    </style>

    <!-- List TextView style specs. -->
    <style name="list">
        <item name="android:divider">@null</item>
        <item name="android:dividerHeight">0dp</item>
        <item name="android:orientation">vertical</item>
    </style>

    <!-- Article title TextView style specs. -->
    <style name="article_title">
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">3</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">26sp</item>
    </style>

    <!-- Article author TextView style specs. -->
    <style name="article_author">
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textColor">#800000</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Article section TextView style specs. -->
    <style name="article_section">
        <item name="android:gravity">center</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">italic</item>
    </style>

    <!-- Article date TextView style specs. -->
    <style name="article_date">
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textSize">12sp</item>
    </style>

    <!-- Article URL TextView style specs. -->
    <style name="article_url">
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
        <item name="android:paddingBottom">18dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">italic</item>
    </style>

</resources>
